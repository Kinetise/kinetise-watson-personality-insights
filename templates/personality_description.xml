<?xml version="1.0" encoding="UTF-8"?>
<rss xmlns:k="http://kinetise.com">
    <channel>
        {% for item in items %}
        <item>
            <id><![CDATA[{{ item.id }}]]></id>
            <name><![CDATA[{{ item.name }}]]></name>
            {% if item.value %}
            <value><![CDATA[{{ item.value }}]]></value>
            {% endif %}
            <type>{{ item.type }}</type>
            {% if item.graphURL %}
            <graphURL>{{ item.graphURL }}</graphURL>
            <showGraph>true</showGraph>
            {% else %}
            <showGraph>false</showGraph>
            {% endif %}
            <hasChildren>{{ item.hasChildren }}</hasChildren>
        </item>
        {% endfor %}
    </channel>
</rss>
